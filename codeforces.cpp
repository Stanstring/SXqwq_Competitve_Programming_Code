#include <bits/stdc++.h>
#define int long long
using namespace std;
typedef pair<int,int> PAIR;
constexpr int N = 1000010;
int T;
int a[N],sum[N];
PAIR p[N];
void solve()
{
    
}
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    cin>>T;
    while(T--)
    {
        solve();
    }
}